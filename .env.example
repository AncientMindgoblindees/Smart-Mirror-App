# VITE_MIRROR_ENV: Optional; defaults to production away from localhost.
# Accepted values: production, development
VITE_MIRROR_ENV="development"

# VITE_MIRROR_API_TOKEN: Optional browser default for authenticated mirror APIs.
# Users can also set this from the companion app settings UI.
VITE_MIRROR_API_TOKEN=""
